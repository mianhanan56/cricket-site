{
  "name": "@crex/worker-crex",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — edge proxy + cache in front of crex.com's internal APIs",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "probe": "./scripts/probe.sh",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.4.0",
    "wrangler": "^4.120.0"
  }
}
